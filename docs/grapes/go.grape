---
phase: env
depend_executable:
  binary: go
  search_paths:
    - /usr/local/go/bin
  version_args:
    - version
  version_regex: "go([0-9]+\\.[0-9]+(?:\\.[0-9]+)?)"
---

--#ifdef PWSH

--#elif NUSHELL

--#else
export GOPATH="${GOPATH:-/usr/local/go}"
export PATH="$GOPATH/bin:$PATH"
--#endif
