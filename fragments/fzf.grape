---
phase: main
depend_executable:
  binary: fzf
  version_args:
    - --version
  version_regex: "([0-9]+\\.[0-9]+\\.[0-9]+)"
---
#ifdef BASH
eval "$(fzf --bash)"
#endif

#ifdef ZSH
source <(fzf --zsh)
#endif
